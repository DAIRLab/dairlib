package dairlib;

struct lcmt_controller_failure
{
  int64_t utime;
  int32_t error_code;         // determines which kind of failure mitigation (1: complete shutdown, 2: pure damping)
  string error_name;
  string controller_channel;
}
