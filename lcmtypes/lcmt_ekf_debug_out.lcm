package dairlib;

struct lcmt_ekf_debug_out
{
  int64_t utime;

  int32_t n_x;
  int32_t n_theta;
  int32_t n_p;

  // only include the top 3 rows of X since everything else is constant
  float X_prop[3][n_x];
  float Theta_prop[n_theta];
  float P_prop[n_p][n_p];
  float X_corr[3][n_x];
  float Theta_corr[n_theta];
  float P_corr[n_p][n_p];
}
