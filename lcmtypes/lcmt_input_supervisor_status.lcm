package dairlib;

struct lcmt_input_supervisor_status
{
  int64_t utime;        // timestamp in microseconds
  string active_channel;
  boolean shutdown;
  int32_t num_status;
  string status_names[num_status];
  boolean status_states[num_status];
}