package dairlib;

struct lcmt_input_supervisor_status
{
  int64_t utime;        // timestamp in microseconds
  int64_t status;
  boolean shutdown;
  boolean vel_limit;
  boolean act_limit;
  boolean act_delay;
}