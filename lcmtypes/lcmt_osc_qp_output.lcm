package dairlib;

struct lcmt_osc_qp_output
{
  int32_t u_dim;
  int32_t lambda_c_dim;
  int32_t lambda_h_dim;
  int32_t v_dim;
  int32_t epsilon_dim;
  float solve_time;
  float u_sol[u_dim];
  float lambda_c_sol[lambda_c_dim];
  float lambda_h_sol[lambda_h_dim];
  float dv_sol[v_dim];
  float epsilon_sol[epsilon_dim];
}
