load("@drake//tools/lint:lint.bzl", "add_lint_tests")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "id_mpc_costs",
    srcs = glob(["*.cc"]),
    hdrs = glob(["*.h"]),
    deps = [
        "//solvers:nonlinear_cost",
        "//systems/controllers/id_mpc/core:id_mpc_core",
    ],
)