"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import drake.lcmt_planar_gripper_finger_status

class lcmt_planar_gripper_status(object):
    __slots__ = ["utime", "num_fingers", "finger_status"]

    __typenames__ = ["int64_t", "int8_t", "drake.lcmt_planar_gripper_finger_status"]

    __dimensions__ = [None, None, ["num_fingers"]]

    def __init__(self):
        self.utime = 0
        self.num_fingers = 0
        self.finger_status = []

    def encode(self):
        buf = BytesIO()
        buf.write(lcmt_planar_gripper_status._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qb", self.utime, self.num_fingers))
        for i0 in range(self.num_fingers):
            assert self.finger_status[i0]._get_packed_fingerprint() == drake.lcmt_planar_gripper_finger_status._get_packed_fingerprint()
            self.finger_status[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != lcmt_planar_gripper_status._get_packed_fingerprint():
            raise ValueError("Decode error")
        return lcmt_planar_gripper_status._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = lcmt_planar_gripper_status()
        self.utime, self.num_fingers = struct.unpack(">qb", buf.read(9))
        self.finger_status = []
        for i0 in range(self.num_fingers):
            self.finger_status.append(drake.lcmt_planar_gripper_finger_status._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if lcmt_planar_gripper_status in parents: return 0
        newparents = parents + [lcmt_planar_gripper_status]
        tmphash = (0xec829b900fe84fe3+ drake.lcmt_planar_gripper_finger_status._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if lcmt_planar_gripper_status._packed_fingerprint is None:
            lcmt_planar_gripper_status._packed_fingerprint = struct.pack(">Q", lcmt_planar_gripper_status._get_hash_recursive([]))
        return lcmt_planar_gripper_status._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", lcmt_planar_gripper_status._get_packed_fingerprint())[0]

